# Optional: public site URL used in Supabase email links (signup confirm, etc.).
# Set on Vercel Production so links never point at localhost when you dev locally.
# Example: https://your-app.vercel.app
# VITE_APP_ORIGIN=
